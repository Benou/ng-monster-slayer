import { TupleValuesPipe } from './tuple-values.pipe';

describe('TupleValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new TupleValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
